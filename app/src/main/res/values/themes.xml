<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ComposeBooking" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>

        <item name="windowSplashScreenAnimatedIcon">@drawable/transparent_rect</item>

        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>

        <item name="postSplashScreenTheme">@style/Theme.ComposeBooking</item>
    </style>
</resources>